import {notFound} from "next/navigation";import {db} from "../../../server/db";import {requireUser} from "../../../server/auth";import {pageDocumentSchema} from "../../../domain/cms";import {HotelShell} from "../../../components/hotel-shell";import {CmsRenderer} from "../../../components/cms-renderer";
export const dynamic="force-dynamic";export const metadata={title:"Draft preview",robots:{index:false,follow:false}};
export default async function Preview({params}:{params:Promise<{id:string}>}){const {property}=await requireUser("website.edit");const {id}=await params;const page=await db.page.findFirst({where:{...(id==="settings"?{slug:"home"}:{id}),propertyId:property.id},include:{revisions:{orderBy:{version:"desc"},take:1}}});if(!page?.revisions[0])notFound();return <HotelShell preview><main id="main"><CmsRenderer document={pageDocumentSchema.parse(page.revisions[0].snapshot)} propertyId={property.id}/></main></HotelShell>;}
